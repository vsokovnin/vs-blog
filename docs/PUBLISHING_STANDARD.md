# Стандарт публикации статей в блоге

Документ описывает полный workflow публикации статей в блоге vs-blog (Astro 5 + MDX).

**Навигация:** [← BLOG.md (центр управления)](../BLOG.md)

**Предварительный этап:** Статья написана по правилам [BLOG_WRITING_RULES.md](../../../content/projects/sokovnin-me/BLOG_WRITING_RULES.md)

---

## Workflow публикации (6 шагов)

```
Шаг 1. Создать .mdx     → src/data/post/{slug}.mdx
Шаг 2. Проверить local  → npm run dev
Шаг 3. Собрать          → npm run build
Шаг 4. Задеплоить       → git add → commit → push
Шаг 5. Проверить prod   → sokovnin.me/blog/{slug}
Шаг 6. Обновить статус  → BLOG.md: 🔧 → ✅
```

---

## Шаг 1. Создать .mdx файл

### 1.1 Источник метаданных

> **КРИТИЧНО:** Метаданные берутся из блока `<!-- PUBLISHING_METADATA -->` в конце статьи.
> Этот блок заполняется на этапе написания по правилам COPY, DON'T TRANSFORM.

**Где найти блок:**
```markdown
<!-- PUBLISHING_METADATA: НЕ публикуется в теле статьи -->
## 📋 Метаданные для публикации
| Поле | Значение | Источник |
...
<!-- /PUBLISHING_METADATA -->
```

### 1.2 Маппинг полей: Блок метаданных → Astro frontmatter

| Поле в блоке метаданных | → Astro frontmatter | Действие |
|-------------------------|---------------------|----------|
| Title | `title` | **КОПИРОВАТЬ** точно |
| Slug | имя файла | `{slug}.mdx` |
| Category | `category` | Lowercase: `strategy` |
| Excerpt | `excerpt` | **КОПИРОВАТЬ** из блока метаданных |
| ReadingTime | — | Не используется (авто) |
| PublishedAt | `publishDate` | ISO: `2026-01-23T00:00:00Z` |
| FAQ (yaml блок) | `faq` | **КОПИРОВАТЬ** точно |

### 1.3 Что удалять при публикации

| Элемент | Причина | Как найти |
|---------|---------|-----------|
| H1 заголовок | Генерируется из `title` | Первая строка с `#` |
| Блок "Об авторе" | Генерируется из `author` | `**Об авторе:**...` |
| Блок PUBLISHING_METADATA | Технический, не для читателя | `<!-- PUBLISHING_METADATA -->...<!-- /PUBLISHING_METADATA -->` |
| Навигация серии **в конце статьи** | Дублирует навигацию в начале | `**Навигация по серии...` в конце |
| **Ручной TOC** | Генерируется автоматически компонентом `TableOfContents.astro` | `## Содержание` с ссылками `[...]` |

> **КРИТИЧНО:** НЕ добавлять `## Содержание` вручную! Компонент `TableOfContents.astro` автоматически генерирует навигацию из всех H2/H3 заголовков статьи. Ручной TOC создаёт дублирование.

### 1.4 Что СОХРАНЯТЬ при публикации

| Элемент | Почему важно |
|---------|--------------|
| Навигация серии **в начале** | Контекст для читателя |
| Секция "Связь с другими частями серии" | Внутренняя перелинковка |
| FAQ секция в теле | Контент для читателя (дублируется в frontmatter для JSON-LD) |

### 1.4.1 КРИТИЧНО: Навигация серии — запрет blockquote

> **Проблема:** Markdown `>` создаёт `<blockquote>`, который Tailwind Typography стилизует кавычками. Две строки с `>` сливаются в одну.

**❌ НЕПРАВИЛЬНО (вызовет проблемы):**
```markdown
> **Серия «Название» — часть N из M**
> [← Prev](/prev) | [Серия](/pillar) | [Next →](/next)
```

**✅ ПРАВИЛЬНО:**
```markdown
**Серия «Название» — часть N из M**<br/>
[← Prev](/prev) | [Серия](/pillar) | [Next →](/next)
```

**Правило:** Навигация серии НЕ использует `>`. Для переноса строки — `<br/>`.

### 1.5 Чек-лист верификации: Оригинал vs Опубликовано

> **ОБЯЗАТЕЛЬНО:** После публикации проверить соответствие.

| # | Проверка | Как проверить | Pass/Fail |
|---|----------|---------------|-----------|
| V1 | `title` === H1 оригинала | Сравнить строки | |
| V2 | `excerpt` основан на Answer Capsule | Факты из AC, законченное предложение, 150-160 символов | |
| V3 | FAQ questions совпадают | Сравнить вопросы | |
| V4 | FAQ answers совпадают | Сравнить ответы | |
| V5 | Навигация серии сохранена | Блок `**Серия...` в начале (БЕЗ `>`) | |
| V6 | Контент не отредактирован | Diff тела статьи | |

**При любом Fail → откатить и исправить.**

### 1.6 Обязательные поля frontmatter

| Поле | Тип | Лимит | Правила |
|------|-----|-------|---------|
| `title` | string | 50-60 символов | Ключевое слово в начале |
| `publishDate` | ISO Date | — | Формат: `2026-01-23T00:00:00Z` |
| `excerpt` | string | 150-160 символов | Прямой ответ на вопрос статьи |
| `category` | enum | — | `strategy` / `product` / `growth` / `monetization` / `ai` |
| `author` | string | — | `Виктор Соковнин` |
| `draft` | boolean | — | `false` для публикации |

### 1.7 Рекомендуемые поля

| Поле | Тип | Когда использовать |
|------|-----|-------------------|
| `updateDate` | ISO Date | При обновлении опубликованной статьи |
| `tags` | string[] | 3-5 тегов для связанных статей |
| `faq` | array | 3-5 вопросов для JSON-LD разметки |
| `image` | string | Обложка 1200×630px |

### 1.8 Пример frontmatter (COPY, DON'T TRANSFORM)

```yaml
---
# Title КОПИРУЕТСЯ из H1 статьи ТОЧНО
title: "Дизайн-мышление для стратегии: введение в методологию DTS"
publishDate: 2026-01-23T00:00:00Z
# Excerpt КОПИРУЕТСЯ из Answer Capsule (первые 160 символов)
excerpt: "Дизайн-мышление для стратегии (DTS) — это методология разработки корпоративной стратегии, объединяющая дизайн-мышление, бизнес-модель и теорию игр."
category: "strategy"
author: "Виктор Соковнин"
draft: false
# FAQ КОПИРУЕТСЯ из секции "Часто задаваемые вопросы" ТОЧНО
faq:
  - question: "Что такое дизайн-мышление для стратегии?"
    answer: "DTS — методология разработки корпоративной стратегии, объединяющая дизайн-мышление, бизнес-модель и теорию игр. Начинает с понимания клиентов и валидирует гипотезы через эксперименты."
  - question: "Когда следует применять DTS?"
    answer: "В условиях высокой неопределённости: технологические разрывы, новые бизнес-модели, быстро меняющиеся потребности клиентов."
---
```

### 1.9 Куда сохранять

```
/Tech/vs-blog/src/data/post/{slug}.mdx
```

Имя файла = slug статьи (URL). Пример: `dts-introduction.mdx` → `/blog/dts-introduction`

---

## Шаг 2. Проверить локально

```bash
cd /Users/vsokovnin/Cursor/Tech/vs-blog
npm run dev
# Открыть http://localhost:4321/blog/{slug}
```

**Чеклист локальной проверки:**
- [ ] Страница открывается без ошибок
- [ ] H1 только один (из title, не из тела)
- [ ] Автор отображается в шапке
- [ ] Категория корректная
- [ ] Таблицы рендерятся (если есть)
- [ ] JSON-LD содержит FAQ (DevTools → `script[type="application/ld+json"]`)

---

## Шаг 3. Собрать проект

```bash
npm run build
```

**Ожидание:** Build complete, нет ошибок.

---

## Шаг 4. Задеплоить

### 4.1 Коммит изменений

```bash
git add src/data/post/{slug}.mdx
git commit -m "feat(blog): add article {slug}"
```

### 4.2 Push в main

```bash
git push origin main
```

**Что происходит:** Push в main автоматически запускает деплой на Vercel → sokovnin.me

### 4.3 Ожидание

Деплой занимает ~1-2 минуты. Статус можно проверить:
- В Vercel Dashboard
- Или дождаться завершения

---

## Шаг 5. Проверить на production

```
https://sokovnin.me/blog/{slug}
```

**Чеклист production:**
- [ ] Страница доступна (не 404)
- [ ] Контент корректный
- [ ] FAQ в JSON-LD (DevTools → `script[type="application/ld+json"]`)
- [ ] Open Graph работает (проверить в [Facebook Debugger](https://developers.facebook.com/tools/debug/) или [Twitter Card Validator](https://cards-dev.twitter.com/validator))

---

## Шаг 6. Обновить статус в BLOG.md

После успешной проверки на production обновить [BLOG.md](../BLOG.md):

**Изменить статус статьи:**
```
🔧 → ✅
```

**Добавить ссылку на production (если нужно).**

---

## Справочник

### Категории

| Slug | Отображаемое имя |
|------|------------------|
| `strategy` | Стратегия |
| `product` | Продукт |
| `growth` | Рост |
| `monetization` | Монетизация |
| `ai` | AI |

> Можно указывать любым регистром — нормализуется автоматически.

### Связанные файлы

| Файл | Назначение |
|------|------------|
| `src/content/config.ts` | Zod-схема валидации frontmatter |
| `src/utils/categories.ts` | Маппинг категорий slug → name |
| `src/components/seo/FAQJsonLd.astro` | Генерация JSON-LD для FAQ |
| `src/components/blog/SinglePost.astro` | Шаблон отображения статьи |

### Требования к контенту

- **Слов:** 1,800–2,200 (оптимум), минимум 1,500
- **H2 заголовков:** 4-7
- **Статистик:** 1 на ~150 слов
- **Внутренние ссылки:** 3-5 на другие статьи блога
- **Внешние ссылки:** 3-4 авторитетных источника (мин. 1 RU + 1 INT)

---

## Чеклист: полный цикл публикации

### Шаг 1: Создание
- [ ] .mdx файл создан в `src/data/post/`
- [ ] Frontmatter заполнен (все обязательные поля)
- [ ] H1 НЕ включён в тело
- [ ] FAQ перенесён в frontmatter
- [ ] Блок "Об авторе" НЕ включён

### Шаг 2-3: Проверка и сборка
- [ ] `npm run dev` — нет ошибок
- [ ] Страница корректно отображается
- [ ] `npm run build` — успешная сборка

### Шаг 4: Деплой
- [ ] `git add` + `git commit`
- [ ] `git push origin main`

### Шаг 5: Верификация
- [ ] Страница доступна на sokovnin.me/blog/{slug}
- [ ] JSON-LD корректный

### Шаг 6: Финализация
- [ ] Статус в BLOG.md обновлён: 🔧 → ✅

---

*Документ обновлён: январь 2026*
